class MyCard{
final String ? docid;
  final String ? card_type;
final String ? card_image;
final String ? userid;
final String ? connected_profile_uid;
final String ? connected_profile_username;

MyCard({this.card_type,this.card_image,this.userid,this.connected_profile_uid,this.connected_profile_username,this.docid});

}